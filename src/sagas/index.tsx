import {all} from 'redux-saga/effects';
// import {onboardingDetailsWatcher} from '../pages/Onboarding/saga';

// Redux Saga: Root Saga
export function* rootSaga() {
  yield all([
    // onboardingDetailsWatcher()
  ]);
}
